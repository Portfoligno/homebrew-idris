name: brew pr-pull
on:
  pull_request_target:
    types: [labeled]

defaults:
  run:
    shell: sh

env:
  HOMEBREW_DEVELOPER: 1
  HOMEBREW_GITHUB_ACTIONS: 1
  HOMEBREW_NO_AUTO_UPDATE: 1
  HOMEBREW_NO_INSTALL_FROM_API: 1

concurrency:
  group: publish-${{ github.event.pull_request.number }}

jobs:
  pr-pull:
    if: contains(github.event.pull_request.labels.*.name, 'pr-pull')
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: read
      contents: write
      issues: read
      pull-requests: write
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up git
        uses: Homebrew/actions/git-user-config@main

      - name: Pull bottles
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PULL_REQUEST: ${{ github.event.pull_request.number }}
        run: >
          brew pr-pull
          --debug
          --tap="$GITHUB_REPOSITORY"
          --workflows=tests.yml
          "$PULL_REQUEST"

      - name: Push commits
        uses: Homebrew/actions/git-try-push@main
        with:
          branch: main

      - name: Create versioned formula and repackage bottles
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          version=$(sed -n 's/^  version "\([^"]*\)".*/\1/p' Formula/idris2-pack.rb)
          python3 scripts/create-versioned-formula.py \
            --formula Formula/idris2-pack.rb \
            --version "$version" \
            --output "Formula/idris2-pack@${version}.rb" \
            --release-tag "idris2-pack-${version}"

      - name: Commit and push versioned formula
        run: |
          version=$(sed -n 's/^  version "\([^"]*\)".*/\1/p' Formula/idris2-pack.rb)
          git add "Formula/idris2-pack@${version}.rb"
          git commit -m "idris2-pack@${version}: versioned formula snapshot"
          git push origin main

      - name: Delete branch
        if: github.event.pull_request.head.repo.fork == false
        env:
          BRANCH: ${{ github.event.pull_request.head.ref }}
        run: git push --delete origin "$BRANCH"
