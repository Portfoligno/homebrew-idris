name: brew test-bot
on:
  push:
    branches:
      - main
      - update/**

defaults:
  run:
    shell: sh

env:
  HOMEBREW_DEVELOPER: 1
  HOMEBREW_GITHUB_ACTIONS: 1
  HOMEBREW_NO_AUTO_UPDATE: 1
  HOMEBREW_NO_INSTALL_FROM_API: 1

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test-bot:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, macos-15]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    permissions:
      actions: read
      checks: read
      contents: read
    steps:
      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Homebrew Bundler RubyGems
        uses: actions/cache@v5
        with:
          path: ${{ steps.set-up-homebrew.outputs.gems-path }}
          key: ${{ matrix.os }}-rubygems-${{ steps.set-up-homebrew.outputs.gems-hash }}
          restore-keys: ${{ matrix.os }}-rubygems-

      - name: Cleanup before
        run: brew test-bot --only-cleanup-before

      - name: Setup
        run: brew test-bot --only-setup

      - name: Tap syntax
        run: brew test-bot --only-tap-syntax

      - name: Extract formula from branch
        id: formula
        if: startsWith(github.ref, 'refs/heads/update/')
        run: |
          # Branch is update/<formula>-<version>; strip prefix and last hyphenated segment
          name="${GITHUB_REF#refs/heads/update/}"
          name="${name%-*}"
          echo "name=$name" >> "$GITHUB_OUTPUT"

      - name: Build bottles
        id: build
        if: startsWith(github.ref, 'refs/heads/update/')
        run: >
          brew test-bot
          --only-formulae
          --only-json-tab
          --skip-dependents
          --testing-formulae=${{ steps.formula.outputs.name }}
        timeout-minutes: 90

      - name: Upload bottles as artifact
        if: always() && steps.build.outcome == 'success'
        uses: actions/upload-artifact@v7
        with:
          name: bottles_${{ matrix.os }}
          path: '*.bottle.*'
          if-no-files-found: error
